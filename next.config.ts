import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // serverComponentsExternalPackages Next.js 15+ icin kaldirildi
  // Prisma ve bcryptjs otomatik olarak server-side kullanilir
}

export default nextConfig